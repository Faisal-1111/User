// client-forwarding.js

// Function to forward documents to a client
function forwardDocument(document, client) {
    // Logic to forward the document
    console.log(`Document: ${document} has been forwarded to client: ${client}`);
}

// Example usage
const doc = 'Project Proposal.pdf';
const clientName = 'ABC Corp';
forwardDocument(doc, clientName);
